name: smart_life_organizer_app
description: A Flutter app to help users organize their life and improve productivity using AI.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3 # For making HTTP requests
  provider: ^6.0.0 # For state management
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/ # Add your asset images here
    - assets/icons/   # Add your asset icons here

  # Add any additional configuration for fonts or other assets here.